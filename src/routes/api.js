const router = require('express').Router();

// const WhatsappCtr = require('../controllers/whatsapp');

// router.route('/whatsapp/index').get(WhatsappCtr.index);

// router.route('/whatsapp/send').post(WhatsappCtr.send);

module.exports = router;
