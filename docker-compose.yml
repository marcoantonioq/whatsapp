version: "3.3"

services:
  whatsapp:
    restart: always
    dns:
      - 8.8.8.8
    build:
      context: .
      dockerfile: Dockerfile
    image: whatsapp
    container_name: whatsapp
    ports:
      - 3010:3010
    volumes:
      - ./:/app